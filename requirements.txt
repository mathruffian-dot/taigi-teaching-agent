# 臺語教材 AI Agent Python 相依套件
pymupdf>=1.24.0
pillow>=10.3.0
requests>=2.31.0
jinja2>=3.1.4          # 預留給 HTML 樣板化重構（目前 material_generator 仍用 f-string）
python-docx>=1.1.2
pytest>=8.2.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0   # [standard] 提供 reload=True 所需的 watchfiles
python-multipart>=0.0.9
httpx>=0.27.0

# 本地語音辨識引擎 (STT whisper 模式核心；缺少時會默默降級為 dummy)
faster-whisper>=1.0.0
